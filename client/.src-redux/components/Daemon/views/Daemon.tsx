import * as React from 'react';

import Layout from '../../layout/layout';

class Daemon extends React.Component {
  public render() {
    return (
      <Layout>Daemon More</Layout>
    );
  }
}

export default Daemon;
